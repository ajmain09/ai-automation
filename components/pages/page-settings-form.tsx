"use client";
import { useState } from "react";
const defaults = [{ key: "name", label: "Name" }, { key: "phone", label: "Phone" }, { key: "address", label: "Address" }, { key: "product", label: "Product" }, { key: "variant", label: "Variant" }, { key: "quantity", label: "Quantity" }];
export function PageSettingsForm({ pageId, initial }: { pageId: string; initial: { currency: string; countryCode: string; defaultLanguage: string; tone: string; requiredOrderFields: string[] } }) { const [value, setValue] = useState(initial); const [message, setMessage] = useState(""); const [loading, setLoading] = useState(false); async function save() { setLoading(true); const response = await fetch(`/api/pages/${pageId}/settings`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(value) }); const body = await response.json(); setLoading(false); setMessage(response.ok ? "Page settings saved." : body.error ?? "Unable to save settings."); } function toggle(key: string) { setValue((current) => ({ ...current, requiredOrderFields: current.requiredOrderFields.includes(key) ? current.requiredOrderFields.filter((item) => item !== key) : [...current.requiredOrderFields, key] })); } return <div className="grid grid-2"><section className="card card-pad"><h2>Page defaults</h2><p className="subtitle">These defaults are isolated to {pageId ? "this page" : "the workspace"}.</p><div style={{ marginTop: 22 }}><div className="grid grid-2"><Field label="Currency" value={value.currency} onChange={(v) => setValue({ ...value, currency: v.toUpperCase() })} /><Field label="Country code" value={value.countryCode} onChange={(v) => setValue({ ...value, countryCode: v.toUpperCase() })} /></div><div className="grid grid-2"><Field label="Default language" value={value.defaultLanguage} onChange={(v) => setValue({ ...value, defaultLanguage: v })} /><Field label="Tone" value={value.tone} onChange={(v) => setValue({ ...value, tone: v })} /></div></div></section><section className="card card-pad"><h2>Required order fields</h2><p className="subtitle">Configuration only in Step 1. The full order AI engine comes later.</p><div style={{ display: "grid", gap: 9, margin: "22px 0" }}>{defaults.map((item) => <label key={item.key} style={{ display: "flex", gap: 10, alignItems: "center", color: "#46574f", fontSize: 12, fontWeight: 650 }}><input type="checkbox" checked={value.requiredOrderFields.includes(item.key)} onChange={() => toggle(item.key)} />{item.label}</label>)}</div>{message && <div className="callout info" style={{ marginBottom: 12 }}>{message}</div>}<div className="form-actions"><button className="button primary" onClick={save} disabled={loading}>{loading ? "Saving…" : "Save settings"}</button></div></section></div>; }
function Field({ label, value, onChange }: { label: string; value: string; onChange: (value: string) => void }) { return <div className="field"><label>{label}</label><input className="input" value={value} onChange={(e) => onChange(e.target.value)} /></div>; }
