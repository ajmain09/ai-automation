export default function OrdersPage() { return <Placeholder title="Orders" description="Order workflows are prepared for a later phase. Page-scoped order sessions, orders, and revisions are already part of the database foundation." label="Order management" />; }
function Placeholder({ title, description, label }: { title: string; description: string; label: string }) { return <main className="workspace"><div className="page-heading"><div><div className="eyebrow">Operations</div><h1>{title}</h1><p className="subtitle">{description}</p></div></div><section className="card"><div className="empty-state"><div className="empty-icon">□</div><h3>{label}</h3><p>This Step 1 placeholder keeps the navigation and domain boundary ready without adding out-of-scope features.</p><span className="status-chip gray"><span className="dot" /> Planned for a later phase</span></div></section></main>; }
