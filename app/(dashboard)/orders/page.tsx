import { getGlobalOrders } from "@/services/orders/service";

export default async function OrdersPage() {
  const result = await getGlobalOrders();
  const orders = Array.isArray(result) ? result : result.items;
  return <main className="workspace"><div className="page-heading"><div><div className="eyebrow">Operations</div><h1>Orders</h1><p className="subtitle">Confirmed orders across all Pages, sorted by creation time.</p></div></div><section className="card">{orders.length === 0 ? <div className="empty-state"><div className="empty-icon">□</div><h3>No orders yet</h3><p>Orders appear here after backend validation and explicit customer confirmation.</p></div> : <div className="table-wrap"><table><thead><tr><th>Order</th><th>Page</th><th>Customer</th><th>Product</th><th>Total</th><th>Status</th><th>Created</th></tr></thead><tbody>{orders.map((order) => <tr key={order.id}><td>{order.id.slice(0, 8)}</td><td>{"page" in order ? order.page.name : "—"}</td><td><div className="product-name">{order.customerName ?? "Unknown"}</div><div className="field-hint">{order.normalizedPhone ?? "No phone"}</div></td><td>{order.productName ?? "—"} {order.quantity ? `× ${order.quantity}` : ""}</td><td>{order.total ? `${order.total} ${order.currency ?? ""}` : "—"}</td><td><span className={`status-chip ${order.status === "CONFIRMED" ? "green" : order.status === "CANCELLED" ? "red" : "amber"}`}><span className="dot" />{order.status}</span></td><td>{order.createdAt.toLocaleString()}</td></tr>)}</tbody></table></div>}</section></main>;
}
