import Link from "next/link";
import { notFound } from "next/navigation";
import { getPageById } from "@/services/pages/queries";
import { PageSettingsForm } from "@/components/pages/page-settings-form";
export const dynamic = "force-dynamic";
export default async function PageSettingsPage({ params }: { params: Promise<{ pageId: string }> }) { const { pageId } = await params; const page = await getPageById(pageId); if (!page) notFound(); return <main className="workspace"><div className="page-heading"><div><div className="eyebrow"><Link href={`/pages/${page.id}`}>{page.name}</Link> / Settings</div><h1>Page settings</h1><p className="subtitle">Configure defaults and required order fields for this page.</p></div></div><div className="tabs"><Link className="tab" href={`/pages/${page.id}`}>Overview</Link><Link className="tab" href={`/pages/${page.id}/business`}>Business</Link><Link className="tab" href={`/pages/${page.id}/products`}>Products</Link><Link className="tab active" href={`/pages/${page.id}/settings`}>Settings</Link></div><PageSettingsForm pageId={page.id} initial={{ currency: page.settings?.currency ?? "USD", countryCode: page.settings?.countryCode ?? "US", defaultLanguage: page.settings?.defaultLanguage ?? "en", tone: page.settings?.tone ?? "helpful", requiredOrderFields: Array.isArray(page.settings?.requiredOrderFields) ? page.settings.requiredOrderFields.map(String) : ["name", "phone", "address", "product", "variant", "quantity"] }} /></main>; }
