import Link from "next/link";
import { notFound } from "next/navigation";
import { getPageById } from "@/services/pages/queries";
import { ProductForm } from "@/components/pages/product-form";
export const dynamic = "force-dynamic";
export default async function ProductsPage({ params }: { params: Promise<{ pageId: string }> }) { const { pageId } = await params; const page = await getPageById(pageId); if (!page) notFound(); return <main className="workspace"><div className="page-heading"><div><div className="eyebrow"><Link href={`/pages/${page.id}`}>{page.name}</Link> / Products</div><h1>Products & variants</h1><p className="subtitle">Build the page-scoped catalog that future sales conversations can use.</p></div><ProductForm pageId={page.id} /></div><div className="tabs"><Link className="tab" href={`/pages/${page.id}`}>Overview</Link><Link className="tab" href={`/pages/${page.id}/business`}>Business</Link><Link className="tab active" href={`/pages/${page.id}/products`}>Products</Link></div><section className="card"><div className="section-head"><div><h2>Catalog</h2><span className="muted">{page.products.length} product{page.products.length === 1 ? "" : "s"} · variants are nested under each product</span></div></div>{page.products.length === 0 ? <div className="empty-state"><div className="empty-icon">□</div><h3>No products yet</h3><p>Add a product with its first variant to start your catalog.</p></div> : <div className="table-wrap"><table><thead><tr><th>Product</th><th>SKU</th><th>Variant</th><th>Price</th><th>Stock</th><th>Status</th></tr></thead><tbody>{page.products.flatMap((product) => product.variants.length ? product.variants.map((variant) => <tr key={variant.id}><td className="product-name">{product.name}</td><td>{variant.sku}</td><td>{[variant.size, variant.color].filter(Boolean).join(" · ") || "Default"}</td><td>{page.settings?.currency ?? "USD"} {Number(variant.currentPrice).toFixed(2)}</td><td>{variant.stockStatus.replaceAll("_", " ").toLowerCase()}</td><td><span className="status-chip green"><span className="dot" /> Active</span></td></tr>) : [<tr key={product.id}><td className="product-name">{product.name}</td><td>—</td><td>Awaiting variant</td><td>—</td><td>—</td><td><span className="status-chip amber"><span className="dot" /> Draft</span></td></tr>])}</tbody></table></div>}</section></main>; }
